# Отчёт: входная ось (разброс концепта) — MVP concept-dim

Дата: 2026-08-03 (обновлено 2026-08-11: добавлена самая широкая ступень `all`).
Модель: `Qwen/Qwen2.5-3B-Instruct`. Сид: 21 (один прогон).

## Цель
Измерить «разброс концепта» на входе для вложенной лестницы
Theft ⊂ Illegal Activities ⊂ Malicious Use ⊂ **all** (весь SALAD) и проверить
ожидаемый порядок `лист < задача < домен < весь`.

## Установка
- Данные: SALAD `base_set`, **стратифицированная** выборка N=900 на ступень
  (пропорции листьев сохранены, seed=21).
- Активации: residual stream на входе блока L, **последний токен**, снятие пер-промптно
  (`extract_activations.py`, паттерн `get_mean_activations`).
- Метрики (`input_spread.py`): среднее попарное `1−cos` (raw / centered),
  **participation ratio** PR = `(Σλ)²/Σλ²` центрированной ковариации, Vendi Score.

## Результат: PR по слоям

| Слой | theft (лист) | illegal (задача) | malicious (домен) | **all (весь)** | Порядок |
|---|---|---|---|---|---|
| **6**  | 10.55 | 11.77 | 12.87 | **14.24** | ✅ лист < задача < домен < весь |
| **12** | 11.25 | 12.01 | 12.56 | **12.74** | ✅ лист < задача < домен < весь |
| 18 | 11.20 | 10.86 | 11.68 | 12.40 | ✗ лист > задача (но `all` наибольший) |
| 24 | 6.72 | 9.94 | 7.50 | 9.65 | ✗ домен < задача |
| 30 | 9.93 | 14.23 | 11.68 | 15.95 | ✗ (но `all` наибольший) |

Vendi@L12: 1.24 / 1.24 / 1.27 / **1.29**; 1-cos(raw)@L12: 0.0269 / 0.0260 / 0.0288 / **0.0308** — `all` наибольший по всем трём.

## Выводы
1. **На ранне-средних слоях (6 и 12) — чистая монотонная лестница `лист < задача < домен < весь`**,
   `all` наибольший (L12: 11.25 < 12.01 < 12.56 < **12.74**; L6: до **14.24**). Входная ось подтверждается и на 4-й ступени.
   **НО прирост malicious→all крошечный (+0.18 PR@L12)**, тогда как induce-размерность прыгает 7.33 → ≥9:
   у верхнего конца входная PR почти насыщается, а write-сторона (induce) продолжает разворачиваться —
   induce оказывается чувствительнее к широте, чем PR эмбеддингов.
2. **Слой 18 (первый выбор) оказался неудачным** — там лист ≳ задача. Прежняя «неудача»
   объяснялась выбором слоя, а не гипотезой.
3. **Метрика — participation ratio.** `1−cos` (насыщается у 0/1) и Vendi (≈1, тоже
   насыщается из-за общей компоненты) ступени НЕ различают; PR — честная мера размерности
   и даёт сигнал.
4. Стратификация дала детерминированные пропорции, но разворот на L18 не убрала —
   его убрал правильный слой.

## Зафиксированные решения для входной оси
- **Метрика: participation ratio (PR)** центрированных активаций.
- **Слой: L = 12** (в «чистой» зоне, серединнее чем 6; надёжный порядок 11.25 < 12.01 < 12.56).

## Границы
- Один сид (seed=21) — нет оценки устойчивости чисел.
- Поздние слои (24, 30) немонотонны; сигнал есть только на ранне-средних слоях.
- Разница лист↔задача умеренная (~0.8 PR на L12) — лестница пологая, но монотонная.

## Артефакты
- Дампы: `experiments/mvp/artifacts/acts_<rung>_L<layer>.pt` (снимаются GPU-раннером
  `run/2a_extract_activations.sh`, слои 6/12/18/24/30).
- Метрики: `experiments/mvp/scripts/input_spread.py` (CPU).
